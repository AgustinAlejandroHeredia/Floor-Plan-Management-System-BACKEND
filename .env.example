PORT= port / 3000
FRONTEND_URL= frontend base url
APP_BASE_URL= public base URL of this backend, e.g. http://localhost:3000

# FILE STORAGE
# Options: backblaze | local | mongodb
FILE_STORAGE_DRIVER=backblaze
# Required when FILE_STORAGE_DRIVER=local
LOCAL_STORAGE_PATH=./uploads
# Convert images to WebP for smaller storage footprint (local and mongodb drivers only).
# Set to false if your inference pipeline does not support WebP.
# Default: true
WEBP_COMPRESSION_ENABLED=true

# AUTH0

AUTH0_AUDIENCE= custom api identifier
AUTH0_ISSUER_URL= single page aplication domain + slash at the end 
AUTH0_USERINFO_ISSUER_URL= single page aplication domain

AUTH0_CLIENT_ID_M2M= machine to machine or M2M client id
AUTH0_CLIENT_SECRET_M2M= M2M client secret

# MONGO

MONGODB_URI= mongo db url, example: mongodb+srv://User:Pasword@clustername.1rvm4.mongodb.net/DatabaseName?retryWrites=true&w=majority

# BACKBLAZE

BACKBLAZE_KEY_ID= key id generated for the backblaze aplication key
BACKBLAZE_APPLICATION_KEY= key given after creating the application key or bucker
BACKBLAZE_BUCKET_ID= bucket id given by de created bucket
BACKBLAZE_BUCKET_NAME= bucket name
BACKBLAZE_DOWNLOAD_URL=https://f000.backblazeb2.com

# PYTHON

PYTHON_EXECUTABLE= python executable path

# ALIGNMENT
# Comma-separated detected-element labels treated as alignment landmarks (columns).
# The current YOLO models emit no column class, so this is unused today but kept for
# when a structural/column detector is added. Default: column
ALIGN_COLUMN_TYPES=column
# Max concurrent alignment Python processes. Default: 2
ALIGN_MAX_CONCURRENT=2

# EMAILS

MAIL_HOST= host
MAIL_PORT= port
MAIL_SECURE= true / false

MAIL_USER= useremail
MAIL_APP_PASSWORD= password or app pasword

MAIL_FROM="name <example@example.com>"
# MODO PRUEBA (E2E sin Auth0)
# Saltea la validacion de JWT y suplanta al usuario indicado.
# Se ignora si NODE_ENV=production. Dejar apagado salvo para pruebas locales.
#AUTH_TEST_MODE=true
#AUTH_TEST_MODE_USER_ID=<_id de un usuario existente en mongo>
#AUTH_TEST_MODE_EMAIL=test-mode@local
#AUTH_TEST_MODE_ROLE=superadmin

export enum OrganizationRole {
  MEMBER = 'member',
  ADMIN = 'admin',
}

export enum ProjectRole {
  VIEWER = 'viewer',
  CREATOR = 'creator',
}

export enum UserRole {
  NONE = 'none',
  SUPERADMIN = 'super_admin',
}